backend:
  name: git-gateway
  repo: gumivision/gumidreamchurch
  branch: main

# 정적 이미지 파일 저장 위치
media_folder: "public/images"
public_folder: "/images"

collections:
  # 1. 게시판 글 (posts 폴더에 .md 문서 저장)
  - name: "posts"
    label: "게시판 글"
    folder: "posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "제목", name: "title", widget: "string" }
      - { label: "작성일자", name: "date", widget: "datetime" }
      - { label: "대표/첨부 이미지", name: "image", widget: "image", required: false }
      - { label: "내용", name: "body", widget: "markdown" }

  # 2. 함께한 순간들 (gallery 폴더에 .md 문서 저장)
  - name: "gallery"
    label: "함께한 순간들 (갤러리)"
    folder: "gallery"  # 마크다운 문서는 gallery 폴더에 저장 (이미지는 media_folder 설정에 의해 public/images 로 자동 업로드됨)
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "사진 제목", name: "title", widget: "string" }
      - { label: "사진", name: "image", widget: "image" }
